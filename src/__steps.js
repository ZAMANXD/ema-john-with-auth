/**
 * 1. Create firebase project
 * 2. Enable web
 * 3. Enable sign in method
 * 4. Install firebase
 * 5. Set firebase config and export
 *
 */

/**
 * Create UserContext (Auth Context): UserContext --> copmponent name, UserContext provides AuthContext
 * Create AuthContext
 * Set AuthContext.Provider
 * Set the children
 * Export AuthContext to use inside useContext hook
 * Get form data
 * getAuth in the userContext
 */

/**
 *FIREBASE HOSTING
 ----------------------------
 */

/**
 *  ONE TIME FOR EACH COMPUTER
 * ********************************
 * 1. npm i -g firebase-tools
 * 2. firebase login
 *
 * FOR EACH PROJECTS ONE TIME
 * ********************************
 * 1. firebase init
 * 2. Make sure: for public directory: select: build
 * 3. single page application : y
 *
 * FOR EACH DEPLOY
 * 1. npm run build
 * 2. firebase deploy
 */
